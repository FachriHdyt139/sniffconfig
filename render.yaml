services:
  - type: web
    name: sniffconfig
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: SNIFF_BOT=1 gunicorn server:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120
    healthCheckPath: /api/stats
